FUNCTION Main()

    LOCAL cNome := "Tais"
    LOCAL nSalario := "3500.00"
    LOCAL lAtivo := .T.
    LOCAL dAdmissao := cTod("10/01/2024")
    LOCAL cDepto := "RH"

    ? "Nome: ", cNome
    ? "Sal†rio :", nSalario
    ? "Ativo: " , lAtivo
    ? "Admiss∆o: ", dAdmissao
    ? "Departamento: " , cDepto
    
RETURN NIL 
